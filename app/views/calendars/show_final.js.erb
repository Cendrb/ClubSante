$(".calendar_with_week_selector[data-therapy=<%= @calendar.therapy.id %>]").html("<%= escape_javascript(render partial: "calendars/calendar_final_with_week_selector", locals: {calendar: @calendar, beginning_date: @date}) %>");
