$("body").append("<%= escape_javascript(render partial: "form") %>");
$('.exercise_modification_dialog').dialog(
{
    resizable: false,
    modal: true,
    width: '371px',
	close: function(event, ui)
    {
        $(this).dialog('destroy').remove();
        clickedOnExercise = false;
    }
});
