if(confirm('Odhlášování je povoleno pouze <%= Ticket.unsubscribe_time_limit / 60 / 60 %> hodin před cvičením. Pokud se nyní odhlásíte, Váš vstup propadne.'))
{
    $.ajax({
        type: "POST",
        url: "/registering_handler/unsubscribe_from",
        data: { id: <%= @data[:exercise].id %>, beginning_offset: <%= @data[:beginning_offset] %>, force: true, source: "calendar_view", user: <%= @data[:user].id %> },
        dataType: 'script',
        format: 'js',
        success: function(msg){
        }
    });
}