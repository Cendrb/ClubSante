<p id="notice"><%= notice %></p>

<h1>Výpis uživatelů</h1>

<table>
  <thead>
    <tr>
      <th>Jméno</th>
      <th>Email</th>
      <th>Úroveň přístupu</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
      	<td><%= user.full_name %></td>
        <td><%= user.email %></td>
        <td><%= User.get_access_level_string(user.access_level) %></td>
        <td><%= link_to 'Nastavit cíle a oprávnění', admin_edit_user_path(user) %></td>
        <td><%= link_to 'Upravit', edit_user_path(user) %></td>
        <td><%= link_to 'Odstranit', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>
